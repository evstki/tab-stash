# Tab Stash icon

The production icon is the hand-tuned vector in `icons/icon-source.svg`.

It uses a full-bleed violet tile, two receding tab layers, and a high-contrast
stash arrow. The simple geometry remains clear at Chrome’s 16 px toolbar size
and fills the icon canvas without the extra inset of the previous artwork.
